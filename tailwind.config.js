module.exports = {
    darkMode: 'class',
    content: [
      "./dist/**/*.{html,js}", // Scan files in dist folder
      "./src/**/*.css"         // Scan source CSS
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }